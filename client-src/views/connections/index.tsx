import React from 'react';
import ReactDOM from 'react-dom';
import Connections from './Connections';

document.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(<Connections />, document.getElementById('root'));
});

{
  "name": "react-ssr",
  "version": "1.0.0",
  "description": "",
  "main": "server-build/server.js",
  "scripts": {
    "start": "pnpm run build && node ${npm_package_main}",
    "build": "tsc && cp -r ./server/templates ./server-build",
    "dev": "pnpm run build && nodemon -e ts,njk --watch server/ ${npm_package_main}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.13.1",
    "koa-compose": "^4.1.0",
    "koa-router": "^10.0.0",
    "koa-static-server": "^1.5.2",
    "koa-views": "^7.0.1",
    "nunjucks": "^3.2.3",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/koa": "^2.13.3",
    "@types/koa-router": "^7.4.2",
    "@types/koa-static-server": "^1.3.0",
    "@types/nunjucks": "^3.1.4",
    "nodemon": "^2.0.7",
    "typescript": "^4.3.3"
  }
}
